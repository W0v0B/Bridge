# v0.2.5

## Fixes

### Config store migration for new fields

**Problem:** Users with persisted config from before v0.2.4 (when `adbQuickPaths` / `ohosQuickPaths` were added) got `undefined` for these fields because zustand's default shallow merge replaced the entire `config` object with the persisted one, losing newly added defaults. This caused a crash (`Cannot read properties of undefined (reading 'map')`) when the File Manager tried to render the quick access tags.

**Fix:** Added a custom `merge` function to the zustand `persist` config that deep-merges `config` fields, so new defaults are preserved when the persisted state doesn't include them.

### File Manager sort toggle stuck

**Problem:** Ant Design Table's default sort cycle includes a third "cancel" state (`ascend -> descend -> undefined`). When the user clicked a column header three times, `s.order` became `undefined` and the fallback `?? "ascend"` reset the internal state to ascending, but the column header showed no sort indicator — creating a desync.

**Fix:** Added `sortDirections: ["ascend", "descend", "ascend"]` to the Name and Modified columns in both ADB FileManager and OHOS HdcFileManager, so clicking toggles directly between ascending and descending without the cancel state.

### Quick access path deletion removing multiple tags

**Problem:** Two issues: (1) `key={idx}` caused incorrect React reconciliation when removing items from the middle of the array — React would unmount the wrong Tag. (2) Missing `e.preventDefault()` on the Ant Design Tag's `onClose` allowed its built-in close animation to remove the tag from the DOM independently of React state, causing multiple tags to disappear.

**Fix:** Changed `key` to `${qp.label}:${qp.path}` for stable identity, and added `e.preventDefault()` so only the React state update controls removal. Applied to both ADB FileManager and OHOS HdcFileManager.

### Duplicate script event listeners causing double output

**Problem:** Both `ShellPanel` and `QuickCommandsPanel` listened for `script_output` and `script_exit` events, causing script output to appear twice in the terminal when Quick Commands was visible.

**Fix:** Removed the duplicate listeners from `QuickCommandsPanel` — `ShellPanel` handles all script events.

### Upload modal missing quick access paths

**Problem:** The Upload modal in File Manager had no quick access path selector, forcing users to manually type destination paths every time.

**Fix:** Added `quickPaths` prop to the shared `UploadModal` component. Both ADB FileManager and OHOS HdcFileManager now pass their quick access paths to the upload modal, rendering clickable tags above the destination path input.

### Drag-drop overlay not visible when scrolled down

**Problem:** The drag-drop upload overlay was positioned inside the scrollable file list container. When the user scrolled down in the file directory, the overlay scrolled with it and became invisible.

**Fix:** Moved the overlay `div` from inside the scrollable container to the outermost `position: absolute; inset: 0` container, so it always covers the full panel regardless of scroll position. Applied to both ADB FileManager and OHOS HdcFileManager.

### Ant Design `destroyOnClose` deprecation warning

**Problem:** Console showed `Warning: [antd: Modal] 'destroyOnClose' is deprecated. Please use 'destroyOnHidden' instead.`

**Fix:** Replaced `destroyOnClose` with `destroyOnHidden` in `CatModal`, `HdcCatModal`, and `UploadModal`.

### HiLog/tlogcat silent failure on unsupported OHOS devices

**Problem:** Two issues caused tlogcat to show no output on unsupported devices: (1) The stdout parser (`parse_hilog_line`) silently dropped lines that didn't match the hilog regex format — error messages like `/bin/sh: tlogcat: inaccessible or not found` were discarded because they come through stdout (hdc mixes remote stderr into stdout). (2) When the process exited, no exit event was emitted to the frontend, so the green "running" indicator stayed on forever.

**Fix:** Added `parse_tlogcat_line()` with a fallback that treats unparseable non-empty lines as INFO-level entries (matching the ADB module's approach). Backend now emits `hilog_exit` events when either hilog or tlogcat processes exit. Both processes also pipe stderr and emit error output as log entries. Frontend listens for exit events, resets the running state, and shows a warning when the process exits with a non-zero code.

## Files Changed

- `src/store/configStore.ts` — Added custom `merge` function for persist middleware
- `src/components/adb/FileManager.tsx` — Fixed sort toggle, quick access tags, drag-drop overlay, upload quick paths
- `src/components/hdc/HdcFileManager.tsx` — Same fixes as ADB FileManager
- `src/components/shell/QuickCommandsPanel.tsx` — Removed duplicate script event listeners
- `src/components/shared/UploadModal.tsx` — Added `quickPaths` prop, replaced `destroyOnClose`
- `src/components/adb/CatModal.tsx` — Replaced `destroyOnClose` with `destroyOnHidden`
- `src/components/hdc/HdcCatModal.tsx` — Replaced `destroyOnClose` with `destroyOnHidden`
- `src-tauri/src/hdc/hilog.rs` — Added `HilogExit` struct, emit exit events, pipe stderr for both hilog and tlogcat
- `src/hooks/useHdcEvents.ts` — Added `useHilogExitEvents` hook
- `src/components/hdc/HilogPanel.tsx` — Handle exit events to reset running state and show warnings
- `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json` — Version bump to 0.2.5
